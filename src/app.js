const css = require('./app.scss');

import $ from "jquery";
import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render( < h1 > Hello, world!Again < /h1> , document.getElementById("root"));